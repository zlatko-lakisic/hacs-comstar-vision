# Skill body for client.vision_scene_analyzer (session overlay packer).

Reply in plain English text only. No tools, no JSON, no markdown fences.
Follow the caller's three-line classify / home-log / phone format exactly.
