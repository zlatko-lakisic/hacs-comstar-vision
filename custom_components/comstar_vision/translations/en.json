{
  "config": {
    "step": {
      "user": {
        "title": "Comstar Vision (AO Reach)",
        "description": "Connect to the ADA AO engine (:8765) for gate/zone still analysis. Mint a token with appId comstar-vision. Leave Multimodal ready off until AO implements docs/AO_REACH_MULTIMODAL_HANDOFF.md. If the engine requires mTLS, paste an enrollment token during setup.",
        "data": {
          "engine_url": "AO engine base URL",
          "api_token": "API token (ao_…)",
          "enroll_token": "mTLS enrollment token (one-time)",
          "app_id": "Reach appId",
          "ttl_seconds": "Session TTL seconds",
          "enabled_agents": "Selected vision agents",
          "default_model": "Default model hint",
          "default_target_width": "Default still width (px)",
          "multimodal_ready": "AO multimodal images ready (enable after ADA handoff)"
        }
      }
    },
    "abort": {
      "single_instance_allowed": "Only one Comstar Vision entry is allowed."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Comstar Vision options",
        "data": {
          "engine_url": "AO engine base URL",
          "api_token": "API token (ao_…)",
          "enroll_token": "mTLS enrollment token (one-time, leave blank to keep pairing)",
          "app_id": "Reach appId",
          "ttl_seconds": "Session TTL seconds",
          "enabled_agents": "Selected vision agents",
          "default_model": "Default model hint",
          "default_target_width": "Default still width (px)",
          "multimodal_ready": "AO multimodal images ready (enable after ADA handoff)"
        }
      }
    }
  }
}
