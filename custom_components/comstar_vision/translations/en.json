{
  "config": {
    "step": {
      "user": {
        "title": "Comstar Vision — connection",
        "description": "Connect to the ADA AO engine (:8765). Mint an API token with appId comstar-vision. If the engine requires mTLS, paste an enrollment token. AO picks the vision model — do not configure one here.",
        "data": {
          "engine_url": "AO engine base URL",
          "api_token": "API token (ao_…)",
          "enroll_token": "mTLS enrollment token (one-time)",
          "app_id": "Reach appId",
          "ttl_seconds": "Session TTL seconds",
          "default_target_width": "Default still width (px)",
          "multimodal_ready": "AO multimodal images ready"
        },
        "data_description": {
          "enroll_token": "Paste a fresh token from this engine. On success you get a notification naming the server."
        }
      },
      "capabilities": {
        "title": "Comstar Vision — capabilities",
        "description": "{catalog_status} Select agents, MCP servers, skills, and an optional harness. AO orchestrates execution from this overlay.",
        "data": {
          "enabled_agents": "Agents",
          "enabled_mcps": "MCP servers",
          "enabled_skills": "Skills",
          "harness_profile": "Harness profile",
          "session_env": "Session env (KEY=VALUE per line)"
        }
      }
    },
    "error": {
      "enroll_failed": "mTLS enrollment failed: {enroll_error}"
    },
    "abort": {
      "single_instance_allowed": "Only one Comstar Vision entry is allowed."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Comstar Vision — connection",
        "description": "Engine connection settings. Paste a new mTLS enrollment token to re-pair (required after changing the engine host). Leave blank to keep the current pairing. AO picks the vision model.",
        "data": {
          "engine_url": "AO engine base URL",
          "api_token": "API token (ao_…)",
          "enroll_token": "mTLS enrollment token (one-time, leave blank to keep pairing)",
          "app_id": "Reach appId",
          "ttl_seconds": "Session TTL seconds",
          "default_target_width": "Default still width (px)",
          "multimodal_ready": "AO multimodal images ready"
        },
        "data_description": {
          "enroll_token": "On success you get a notification naming the server URL. On failure the form shows the error and the previous pairing is left unchanged."
        }
      },
      "capabilities": {
        "title": "Comstar Vision — capabilities",
        "description": "{catalog_status} Select agents, MCP servers, skills, and an optional harness.",
        "data": {
          "enabled_agents": "Agents",
          "enabled_mcps": "MCP servers",
          "enabled_skills": "Skills",
          "harness_profile": "Harness profile",
          "session_env": "Session env (KEY=VALUE per line)"
        }
      }
    },
    "error": {
      "enroll_failed": "mTLS enrollment failed: {enroll_error}"
    }
  }
}
